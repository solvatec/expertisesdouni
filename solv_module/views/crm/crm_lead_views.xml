<?xml version="1.0" encoding="UTF-8"?>
<odoo>
        <!-- inherit partner form view -->
        <record id="view_crm_lead_form_inherit" model="ir.ui.view">
            <field name="name">crm.lead.form.inherit</field>
            <field name="model">crm.lead</field>
            <field name="inherit_id" ref="crm.crm_lead_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='user_id']" position="replace"> 
                    <field name="user_id" string="Chargé de compte" domain="[('share', '=', False)]" context="{'default_sales_team_id': team_id}" widget="many2one_avatar_user"/>
                </xpath>
                <xpath expr="//field[@name='team_id']" position="replace"> 
                    <field name="team_id" string="Pipeline" widget="selection" domain="['|', ('company_id', '=', False), ('company_id', '=', company_id)]"/>
                </xpath>
                <xpath expr="//field[@name='team_id']" position="after"> 
                    <field name="reffered_id" string="Apporté par"/>
                    <field name="is_partner" string="Est un client"/>
                </xpath>              
            </field>
        </record>
</odoo>